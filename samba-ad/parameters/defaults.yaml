# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: samba-ad
  rootgroup: root
  config: '/etc/samba-ad'
  service:
    name: samba-ad
  subcomponent:
    config: '/etc/samba-ad-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
